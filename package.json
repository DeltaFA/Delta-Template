{
  "name": "delta",
  "version": "0.1.0",
  "scripts": {
    "build": "tsx build.ts",
    "dev": "tsx build.ts --folder --to-factorio --skip-version",
    "dev-build": "tsx build.ts --to-factorio --skip-version",
    "dev-link": "tsx build.ts --link --to-factorio --skip-version",
    "release": "tsx build.ts --release"
  },
  "devDependencies": {
    "@pnpm/types": "^10.0.0",
    "@prettier/plugin-lua": "^0.0.3",
    "@types/node": "^20.12.7",
    "archiver": "^7.0.1",
    "factoriomod-debug": "^1.1.42",
    "tsx": "^4.7.2"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "prompts": "^2.4.2",
    "semver": "^7.6.0"
  }
}
